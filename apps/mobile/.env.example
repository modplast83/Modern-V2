# API base URL for the MPBF backend.
# In development on a real device, replace localhost with your machine's LAN IP.
# In production, use your deployed domain (e.g. https://m.modplastic.com)
EXPO_PUBLIC_API_BASE_URL=http://localhost:5000
EXPO_PUBLIC_API_TIMEOUT_MS=30000
